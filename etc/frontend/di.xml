<?xml version="1.0"?>
<!--
    `Http\Context` is built in the primary scope during bootstrap, and a plugin
    declared on it in a module global `di.xml` does not reach the area plugin
    list. Every core plugin on this type is declared per area for the same
    reason - see Magento_Persistent and Magento_CatalogPermissions.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">
    <type name="Magento\Framework\App\Http\Context">
        <plugin name="commerce_cache_vary_policy"
                type="Commerce\CacheVary\Plugin\Framework\App\Http\VaryStringPlugin"
                sortOrder="10"/>
    </type>
</config>
